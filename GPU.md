# GPU.md

## 利用可能なGPUサーバ

2台のGPUサーバが利用可能です。

| 通称 | IPアドレス | 備考 |
|------|------------|------|
| 1号機 | 192.168.100.1 | 現在のマシン |
| 2号機 | 192.168.100.2 | SSH経由でアクセス |

※ 指示やドキュメント内では正式なホスト名の代わりに通称（1号機、2号機）を使用することがあります。

## ネットワーク構成

- 1号機と2号機は100GbEで接続
- GPUDirectRDMAが使用可能

## 2号機への接続

SSH経由で2号機にアクセスできます。

```bash
ssh ubuntu@192.168.100.2
```

- パスワードなしでsudoを使用可能

## P2P通信の無効化（必須）

**現在P2P通信に問題があるため、GPUを使用する作業を行う前に必ずACLでP2Pを無効化すること。**

```bash
export NCCL_P2P_DISABLE=1
```

この環境変数を設定してからGPU関連のタスクを実行する。

## GPUタスク実行前の確認

GPUを使用するタスクを実行する前に、以下のコマンドでGPUが使用中でないか確認すること。

```bash
nvidia-smi
ps aux | grep -E 'python|torch|cuda'
```

- 他のプロセスがGPUを使用中の場合は、タスクの実行を控える
- 判断に迷う場合はユーザに確認する
