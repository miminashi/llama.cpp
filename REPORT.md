# REPORT.md

## レポート作成ルール

- レポートはプロジェクトルート以下の `report` ディレクトリに作成する
- レポートのタイトルは日本語で記載する
- レポートには日時（分まで）を入れる
- レポートのファイル名は `レポート名_yyyy-mm-dd_hhmmss.md` にする（ファイル名のレポート名は英語）
- 実験やタスクの前提条件・目的は専用のセクションを設けて記載する
- 実験の再現方法（手順・コマンド等）を記載する
- 実験に際して参照した過去のレポートがある場合は、そのレポートへのリンクを記載する

### 例

```
report/
  distributed_inference_report_2026-01-30_143052.md
```

ファイル内の例:
````markdown
# 分散推論実装レポート

- **実施日時**: 2026年1月30日 14:30

## 前提・目的

Tesla P100 GPUクラスターを使用した分散推論の実現可能性を検証する。

- 背景: 単一GPUではメモリ不足により大規模モデルの推論ができない
- 目的: 2台のGPUサーバを使用してモデルを分割し、推論を実行する
- 前提条件: 1号機と2号機が100GbEで接続されていること

## 再現方法

1. 依存パッケージのインストール
   ```bash
   pip install -r requirements.txt
   ```

2. 2号機でワーカープロセスを起動
   ```bash
   ssh ubuntu@192.168.100.2 "python worker.py --port 5000"
   ```

3. 1号機で推論を実行
   ```bash
   python run_inference.py --worker 192.168.100.2:5000
   ```
````
